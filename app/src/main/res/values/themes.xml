<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Pokedecs" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.Pokedecs.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">
            @color/splash_background
        </item>

        <item name="windowSplashScreenAnimatedIcon">
            @drawable/vector_avatar_placeholder
        </item>

        <item name="postSplashScreenTheme">
            @style/Theme.Pokedecs
        </item>
    </style>
</resources>